49927398716
12121212
1907039
32165
19

im facing this issue with my luhn algorithm checker written in c++.
Code:
```cpp
#include<iostream>
#include<fstream>
#include<string>
#include<cmath>

using namespace std;

int main() {
	ifstream fd("duom.txt");
		long long nr;
		int A[20];
		fd >> nr;
		
		// get len
		int n = 0;
		while (nr >= 0) {
			long long deg = pow(10, n);
			int skaicius = nr % (deg * 10) / deg;
			A[n] = skaicius;
			n++;
			
			if ((skaicius + 1) * deg > nr) break;	
		}
		
		// reverse
		for (int i=0; i<n/2; i++) {
			int x = A[i];
			A[i] = A[n-1-i];
			A[n-1-i] = x;
		}
		
		// luhn check
		int sum = 0;
		for (int i=0; i<n; i++) {
			cout << A[i] << endl;
			int sk = A[i];
			if ((i+1) % 2 == 0) sk *= 2;// daugyba is 2
			
			if (sk >= 10 && sk < 100) { // dvizenklis
				int x = sk / 10;
				int y = sk % 10;
				cout << x+y << endl;
				sum += x + y;
			}
			else sum += sk;
			cout << "-------------" << endl;
		}
		cout << "sum: " << sum;
	return 0;
}
```
Input: `12121212`
Output:
```
1
-------------
2
-------------
1
-------------
2
-------------
1
-------------
2
-------------
1
-------------
2
-------------
sum: 20
```

Explanation: it would seem that code `12121212` is correct, because it's luhn sum of digits ends with a 0, however actually this code is invalid - the correct code would be `121212120`, but since the 0 doesnt change the sum, previously mentioned number is incorrectly labeled as correct. How do I fix my error.

// 

use luhn checker to check if this is a valid code: 12121212